'use strict';

console.log('test 2 script');
// parity outlier 
function outlier(arr) {

	let check = arr;
	// let data = Char[arr].length
	// let Newarr = split('arr')
	let res = splice(arr - 1);

	// console.log(Newarr, "hello bor")

	const dats = function() {
		let f = []
		var p = res.push[f]
		return p;
	}

	console.log(dats, "check function")

	// console.log(data, "this for charIndexAt")

	if (arr.length % 2 == 0) {
		console.log(arr, "genap")
	}

	console.log(arr.length, "data")

	// check the length of an array 
	if (arr.length !== 3) {
		console.log("validate length")

	}
}



outlier([3, 2, 3])
